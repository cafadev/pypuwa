[project]
name = "my-infra"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pypuwa>=0.1.0",
    "pulumi>=3.0.0,<4.0.0",
    "pulumi-azure-native>=3.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
